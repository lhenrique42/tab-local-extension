export default defineBackground(() => {
  // Background service worker — MV3 event-driven.
  // Full implementation in TASK-003.
  console.warn('[TabLocal] Background service worker initialized (stub).');
});


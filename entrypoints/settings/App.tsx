import { useTheme } from '../../composables/useTheme';

export default function App() {
  useTheme();
  return (
    <div>
      <h1>TabLocal — Settings</h1>
      <p>Settings entrypoint (stub — implemented in TASK-014)</p>
    </div>
  );
}
